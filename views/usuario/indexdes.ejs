<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Usuarios desactivos</title>
    <link rel="stylesheet" href="/styles/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>

<body>

    <header>
        <a href="/menu" class="btn btn-secondary">Volver al menú <i class="fas fa-arrow-left"></i></a>
        <a href="/logout" class="logout-btn">Cerrar Sesión <i class="fas fa-sign-out-alt"></i></a>
    </header>

    <h1>Usuarios desactivos</h1>
    <a href="/crearusuario" class="btn btn-success">Crear empleado <i class="fas fa-plus"></i></a>
    <a href="/usuarios" class="btn btn-info">Ver lista de usuarios <i class="fas fa-eye"></i></a>
    <table class="enhanced-table">
        <thead>
            <tr>
                <th>Código</th>
                <th>Nombre</th>
                <th>Apellido</th>
                <th>Correo Electrónico</th>
                <th>Telefono</th>
                <th>Fecha de nacimiento</th>
                <th>Género</th>
                <th>Estado</th>
                <th>Tipo</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <% usuario.forEach((dato)=>{ %>
                <tr>
                    <td>
                        <%= dato.codigo %>
                    </td>
                    <td>
                        <%= dato.nombre %>
                    </td>
                    <td>
                        <%= dato.apellido %>
                    </td>
                    <td>
                        <%= dato.correoelectronico %>
                    </td>
                    <td>
                        <%= dato.telefono %>
                    </td>
                    <td>
                        <%= dato.fecha_nacimiento %>
                    </td>
                    <td>
                        <%= dato.genero %>
                    </td>
                    <td>
                        <%= dato.estado %>
                    </td>
                    <td>
                        <%= dato.tipo %>
                    </td>
                    <td><a href="/activarusuario/<%= dato.codigo %>" class="btn-action btn-view">Activar <i class="fas fa-eye"></i></a></td>
                </tr>
                <% }) %>
        </tbody>
    </table>
</body>